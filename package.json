{
  "name": "peak-empire",
  "version": "0.1.0",
  "private": true,
  "description": "Sales Intelligence Empire Builder",
  "scripts": {
    "dev": "pnpm --filter @peak-empire/web dev",
    "build": "pnpm --filter @peak-empire/web build",
    "functions:serve": "pnpm --filter @peak-empire/functions serve",
    "functions:deploy": "pnpm --filter @peak-empire/functions deploy",
    "init-sheet": "tsx scripts/init-sheet.ts"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "xlsx": "^0.18.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=20"
  }
}
